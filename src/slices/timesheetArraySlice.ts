/**
 * Slice that has reducer and action for timesheet information
 */

/*import { createSlice } from '@reduxjs/toolkit'
import type { PayloadAction } from '@reduxjs/toolkit'
import { TimesheetData } from '../models/models';

const initialState: Array<TimesheetData> = [];

export const timesheetArraySlice = createSlice({
    name: 'timesheet',
    initialState,
    reducers: {
        setListData: (state, action: PayloadAction<TimesheetData[]>) => {
            state = action.payload
        }
    }
})

export const { setListData } = timesheetArraySlice.actions;

export default timesheetArraySlice.reducer;*/